# Format: HH:MM shell_command
#
# Examples:
#   22:30 svc data disable         # matikan data jam 22:30
#   07:00 svc data enable          # hidupkan data jam 07:00
#   23:00 cmd connectivity airplane-mode enable
#   06:00 cmd connectivity airplane-mode disable
#   12:00 echo "lunch time" > /sdcard/log.txt
#
# Command dijalankan setiap kali waktu cocok (tiap hari).
# Awali dengan # untuk nonaktifkan sementara.
